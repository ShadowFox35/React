export interface navElemType {
  text: string;
  link: string;
}

export interface catalogElemType {
  category: string;
  name: string;
  brand: string;
  structure: string;
  weight: string;
  length: string;
  price: number;
  url: string;
}
